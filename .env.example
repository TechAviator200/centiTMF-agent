# Copy to .env and fill in values
# OpenAI (optional - system works without it using deterministic fallbacks)
OPENAI_API_KEY=

# Postgres (used by docker-compose defaults)
POSTGRES_USER=centitmf
POSTGRES_PASSWORD=centitmf
POSTGRES_DB=centitmf

# MinIO (used by docker-compose defaults)
MINIO_ROOT_USER=centitmf
MINIO_ROOT_PASSWORD=centitmf123
